# Changelog

All notable changes to [[.Name]] are documented here. `veduta release vX.Y.Z` turns the
Unreleased section into the release's entry.

## Unreleased

- Project created with `veduta init` (engine [[.Engine]]).
