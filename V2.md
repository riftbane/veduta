# Veduta v2 — piano e avanzamento

## Decisioni

- Controlli della console: D-pad, A, B, Select (menu del gioco), Cancel (indietro), Home
  (torna alla home del dispositivo; il gioco non lo riceve).
- Giochi scritti in Lua (sottoinsieme di Lua 5.4) eseguiti da una VM scritta in Go nel
  motore: solo stdlib, niente cgo, deterministica su amd64 e arm64.
- Nessun gioco demo: niente template giocabile, niente demo né tinycube nell'immagine di
  VedutaOS. I giochi di prova si fanno in locale e non si committano.
- I giochi Go restano supportati durante la transizione; poi si decide.
- Un solo runtime (modulo `veduta`) usato da VedutaOS e dall'SDK; il gioco dichiara il
  livello di API che richiede.
- Sviluppo su `main`; release candidate `v2.0.0-rc.N` sul canale beta; la v1 resta sui tag.
- Scheda di riferimento: Orange Pi Zero 2W 1 GB.

## Fase 1 — Prototipo (motore)

- [x] 1.1 Input a 8 pulsanti: `sim.Input`, scenari, pad e tastiera, docs
- [x] 1.2 Template ridotto a scheletro vuoto; i test del motore usano `internal/testgame`
- [x] 1.3 VM Lua: lexer, parser, compilatore, tabelle, metatabelle, closure, errori con riga
- [x] 1.4 Librerie Lua: base, string, table, math su `gmath`; niente io/os; iterazione
  deterministica; limite di istruzioni per tick
- [x] 1.5 Runtime dei giochi Lua: `script` in `veduta.json`, API (scena, entità, input,
  HUD, trace, RNG); il tool li esegue senza `go build`
- [x] 1.6 Scheletro Lua per `veduta init`; verifica con un gioco di prova locale
- [x] 1.7 Simulator `veduta sim` su Windows: finestra Win32, RGB565, scala intera,
  tastiera → pulsanti (verificato dalla CI su Windows; da provare a mano)
- [x] 1.8 Pad su Windows: XInput e WinMM (mappatura testata; da provare con un pad vero)
- [x] 1.9 Overlay del budget (ms, triangoli) e registrazione della partita come scenario
- [x] 1.10 Ricarica al volo di asset e script
- [x] 1.11 Bench Lua contro Go (amd64, arm64 in qemu): 200 entità che si muovono e
  cercano sovrapposizioni, update per tick Lua 1,2 ms contro Go 0,55 ms su amd64 (2,2×),
  16,8 contro 9,0 ms in qemu arm64 (1,9×); `VEDUTA_BENCH=1 go test -run TestLuaAgainstGo -v ./script/`
- [x] 1.12 Release `v2.0.0-rc.1` (pre-release su GitHub: Linux amd64/arm64, Windows; `veduta update --channel beta`)

## Fase 2 — Runtime e VedutaOS

- [x] 2.1 Livello di API: dichiarato dal gioco, verificato dal runtime
- [x] 2.2 Immagine senza giochi demo (via template e tinycube da workflow e `demos.env`)
- [x] 2.3 VedutaOS esegue i giochi Lua con il runtime di sistema (`vshell play`, processo
  figlio; modulo del motore `github.com/riftbane/veduta/v2` da `v2.0.0-rc.2`); e2e su QEMU
  con un gioco Lua creato da `veduta init`
- [x] 2.4 Dashboard con i nuovi controlli: D-pad, A gioca, Select apre il menu (POWER OFF);
  Home sulla dashboard della console non fa nulla
- [x] 2.5 Porting Orange Pi Zero 2W: U-Boot, kernel mainline, overlay del pannello SPI,
  pulsanti su `gpio-keys` (stessa immagine dei Pi: U-Boot e kernel 6.18 di Armbian,
  `extlinux.conf`, DTB modificato in Go con pannello su SPI1, retroilluminazione, 9 pulsanti,
  HDMI spento; cablaggio identico ai Pi, `docs/quickstart-orangepi.md`; nessun emulatore:
  verificato solo al build)
- [ ] 2.6 Prova sull'hardware: su Raspberry Pi 5, alla consegna finale di tutte le fasi
  (Orange Pi Zero 2W rimandato: la scheda non c'è ancora). Controllato senza hardware per il
  Pi 5: driver built-in o nell'initramfs, overlay applicati al DTB vero con `fdtoverlay`;
  corretti i pulsanti sui Pi in VedutaOS `v0.5.0-rc.3`

## Fase 3 — SDK per Windows

- [x] 3.1 Installer plug&play (`veduta.exe` nel PATH, niente Go per i giochi Lua):
  `install.ps1` (`irm … | iex`), provato dalla CI in Windows PowerShell e pwsh
- [x] 3.2 Estensione VS Code: nuovo gioco, F5 nel Simulator, build, deploy
  (`editors/vscode`, JS senza dipendenze; CI: `node --test`, `vsce package`, VS Code vero
  sotto Xvfb; `veduta-vscode.vsix` in ogni release, installato da `install.ps1` se c'è `code`)
- [x] 3.3 Autocompletamento: stub Lua generati dall'API, JSON Schema dei formati
  (`.veduta/` e `.vscode/` da `init`/`upgrade`; stub scritti a mano e confrontati con la VM,
  schemi generati da `asset/source.go`; provato con lua-language-server 3.19.1 `--check`)
- [x] 3.4 Debug Lua (Debug Adapter Protocol): `veduta dap` e tipo di debug `veduta`
  nell'estensione (F5); hook nella VM solo con un debugger; test del protocollo con race
  detector e breakpoint vero dentro VS Code in CI
- [x] 3.5 Wiki: sito generato da `docs/*.md` (primo gioco, API, controlli):
  https://riftbane.github.io/veduta/ (workflow `site`; `first-game` verificato da un test
  che costruisce il gioco dalla pagina)
- [x] 3.6 `veduta deploy` verso la console (sulla card: la trova per etichetta
  VEDUTA/VEDUTAOS; la console non ha rete)

## Fase 4 — Rilascio

- [x] 4.1 API native per il lavoro pesante (mesh generate a runtime) esposte a Lua:
  `mesh` (parti, quad, triangoli, box con maschera di facce) e `volume` (griglia di blocchi,
  `mesh.voxels` in Go); golden della geometria uguale su arm64
- [ ] 4.2 Release `v2.0.0`: dopo la prova sul Raspberry Pi 5 (2.6). Intanto `v2.0.0-rc.3` (canale
  beta: installer Windows, estensione VS Code col debugger, editor, deploy, sito, mesh) e
  VedutaOS `v0.5.0-rc.3` (immagine unica per Raspberry Pi e Orange Pi Zero 2W)

## Fase 5 — RPG (quello che serve a un gioco grande)

- [x] 5.1 Correzioni della rc.3: snapshot dei giochi Lua (per replay: scena, seed e input,
  verificato sull'hash della traccia); l'estensione (0.3.0) avvisa se `veduta` manca o è una
  v1; `math.deg`, `math.rad` (confrontati con Lua 5.4)
- [x] 5.2 Entità da Lua: `parent` e `hitbox` in `scene.spawn` e come campi, `e:children()`;
  prefab da Lua (`scene.spawn_prefab`, `Context.SpawnPrefab`, stessa posa dei mondi)
- [x] 5.3 HUD con immagini: `hud.image` (una parte di una texture, dimensione, colore,
  specchio), `hud.panel` (nine-slice), `hud.image_size`; `Context.Texture` per Go
- [ ] 5.4 Asset in sottocartelle (nomi unici, errore sui doppioni)
- [ ] 5.5 Release `v2.0.0-rc.4`; wiki aggiornato (limiti tolti, pagina sulla struttura dei
  giochi grandi)
- [ ] 5.6 Salvataggi: API Lua, file per gioco su PC e sulla card; VedutaOS scrive sulla card
  in modo sicuro; scenari con un salvataggio iniziale
- [ ] 5.7 Testo: font Latin-1 (lettere accentate), UTF-8 in `hud.text`, a capo automatico
- [ ] 5.8 Sprite sheet: un materiale a griglia e il fotogramma per entità
- [ ] 5.9 Coroutine nella VM (cutscene e dialoghi scritti in sequenza)
- [ ] 5.10 Audio: amplificatore I2S e cablaggio (da scegliere), mixer deterministico nel
  motore, driver in VedutaOS, audio nel simulatore
